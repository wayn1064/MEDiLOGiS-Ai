steps:
  # 1. 백엔드 빌드
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/medilogis-ai-backend', './backend']
  
  # 2. 백엔드 이미지 푸시
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/medilogis-ai-backend']
    
  # 3. 백엔드 Cloud Run 배포
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'medilogis-ai-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/medilogis-ai-backend'
      - '--region'
      - 'asia-northeast3'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '5001'

  # 4. 프론트엔드 빌드
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/medilogis-ai-frontend', './frontend']
    
  # 5. 프론트엔드 이미지 푸시
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/medilogis-ai-frontend']

  # 6. 프론트엔드 Cloud Run 배포
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'medilogis-ai-frontend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/medilogis-ai-frontend'
      - '--region'
      - 'asia-northeast3'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--port'
      - '80'

images:
  - 'gcr.io/$PROJECT_ID/medilogis-ai-backend'
  - 'gcr.io/$PROJECT_ID/medilogis-ai-frontend'

options:
  logging: CLOUD_LOGGING_ONLY
